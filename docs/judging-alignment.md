# Scout — Hackathon Judging Alignment Matrix

This document maps Scout's architectural components directly to the evaluation criteria for the **SLAB (Self-Learning Agent Browser) Hackathon** hosted by **webcmd**.

---

| Criterion | Max Points | Scout Architectural Implementation | Code Reference |
|---|---|---|---|
| **Usefulness & Impact** | 25 | **Change Detection & Deadline Alerts**: Diffs each run against snapshots, immediately surfacing `closing_soon` deadlines and `new` openings.<br>**Personalized Ranking**: Multi-factor scoring (tags, urgency, location, budget constraints) so students see what's actually relevant first. | [`backend/core/change_detector/change_detector.py`](../backend/core/change_detector/change_detector.py)<br>[`backend/core/matcher/matcher.py`](../backend/core/matcher/matcher.py) |
| **Technical Depth & Recovery** | 20 | **Fault Tolerance**: `tenacity` exponential backoff retries (3 attempts) on all webcmd calls.<br>**Circuit Breaker**: Trips on 3 consecutive failures to avoid infinite error loops.<br>**Safety Timeout**: Approval Gate auto-rejects on timeout so pipeline never hangs.<br>**Cost Controls**: Anthropic Prompt Caching (`cache_control`) + strict `max_tokens` caps (1024 / 512). | [`backend/core/webcmd_adapter/base.py`](../backend/core/webcmd_adapter/base.py)<br>[`backend/core/webcmd_adapter/execute.py`](../backend/core/webcmd_adapter/execute.py)<br>[`backend/core/approval_gate/approval_gate.py`](../backend/core/approval_gate/approval_gate.py)<br>[`backend/core/planner/planner.py`](../backend/core/planner/planner.py) |
| **webcmd Integration** | 20 | **Explore-Once / Reuse-Forever**: webcmd explores unfamiliar pages once, compiles reusable commands, and registers them in the persistence registry.<br>**Live Source Learning**: Real-time `POST /api/sources/learn` flow teaching Scout new domains on the fly. | [`backend/core/webcmd_adapter/`](../backend/core/webcmd_adapter/)<br>[`backend/api/routes/sources.py`](../backend/api/routes/sources.py) |
| **Self-Learning / Reusability** | 15 | **Generalized Engine**: Zero pipeline code is vertical-specific. Student Opportunities and Hotel Price Monitor run on the exact same code with different JSON configs.<br>**Config Template**: Adding a 3rd vertical (e.g. GitHub `good-first-issue` monitor) requires only a folder in `verticals/` with zero backend code changes. | [`backend/verticals/student_opportunities/`](../backend/verticals/student_opportunities/)<br>[`backend/verticals/hotel_price_monitor/`](../backend/verticals/hotel_price_monitor/)<br>[`backend/verticals/_template/`](../backend/verticals/_template/) |
| **UI & Demo Quality** | 10 | **Next.js 15 + Tailwind CSS v4 Dashboard**: Dark glassmorphic interface with circular match score rings, Kanban lifecycle boards, live execution timelines, and interactive simulation triggers. | [`dashboard/src/`](../dashboard/src/) |
| **Responsible Build & Safety** | 10 | **Hard Approval Gate**: Non-bypassable checkpoint physically blocking execution before any `apply`, `submit`, `payment`, or `delete` action.<br>**Audit Trail**: Full logging of decisions (human vs timeout). | [`backend/core/approval_gate/approval_gate.py`](../backend/core/approval_gate/approval_gate.py)<br>[`backend/api/routes/approvals.py`](../backend/api/routes/approvals.py) |
